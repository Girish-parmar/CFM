# Tested versions for the learner environment and the pinned CI job (Python 3.11 and 3.12).
# Dependencies are declared once, in pyproject.toml; this file only pins versions.
# Update: create a fresh venv, `pip install -e ".[dev,boost]" xgboost-cpu`, run `make all`,
# then refresh these pins from `pip freeze` and record the change in CHANGELOG.md.
# Python 3.10 is supported by the library (CI tests it unpinned), but these pins need 3.11+.

# core
numpy==2.4.6
pandas==3.0.6
scipy==1.17.1
scikit-learn==1.9.1
statsmodels==0.15.0
matplotlib==3.11.2
joblib==1.6.0
threadpoolctl==3.7.0
patsy==1.0.3
python-dateutil==2.9.0.post0
contourpy==1.3.3
cycler==0.12.1
fonttools==4.66.0
kiwisolver==1.5.1
pillow==12.3.0
pyparsing==3.1.1
packaging==24.0

# boost extra
xgboost==3.2.0
xgboost-cpu==3.2.0
lightgbm==4.7.0
optuna==5.0.0
SQLAlchemy==2.0.54
alembic==1.20.0
colorlog==6.12.0
tqdm==4.70.1

# dev extra
pytest==9.1.1
pluggy==1.6.0
iniconfig==2.3.0
PyYAML==6.0.1
ruff==0.15.8
