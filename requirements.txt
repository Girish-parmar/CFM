# One command for learners (Python 3.11+), from the repository root:
#     pip install -r requirements.txt
# Dependencies are declared in pyproject.toml; constraints.txt pins the tested versions.
# On Python 3.10 use: pip install -e ".[dev,boost]"
-c constraints.txt
-e .[dev,boost]
